# Security Notes
- Store all secrets as environment variables (Railway variables).
- Run IBKR Gateway (IBeam) on a private host; restrict inbound to your app IP.
- Start in paper trading. Only move to live after burn-in.
- Use private Telegram chat for action buttons.
