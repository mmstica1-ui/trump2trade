# 🎯 הגדרת חשבון IBKR אמיתי - DU7428350

## 🔍 הבעיה שזוהתה
השרת IBKR מחובר כרגע לחשבון דמו `DU1234567` במקום החשבון האמיתי שלך `DU7428350`.

## 💡 הפתרון

### 📋 אופציה 1: סקריפט אוטומטי (מומלץ)

**העתק והרץ בשרת IBKR שלך:**
```bash
./CONFIGURE_REAL_ACCOUNT.sh
```

### 📋 אופציה 2: פקודות ידניות

**1️⃣ הגדר משתני סביבה:**
```bash
export TWS_USERNAME="ilyuwc476"
export TWS_PASSWORD="trump123!"
export IBKR_ACCOUNT_ID="DU7428350"
export TRADING_MODE="paper"
```

**2️⃣ עצור שירותים קיימים:**
```bash
# אם יש PM2:
pm2 delete all

# אם יש Docker:
docker stop $(docker ps -q)

# אם יש Python:
pkill -f python
```

**3️⃣ הפעל מחדש עם הגדרות חדשות:**
```bash
# Docker:
docker-compose up -d

# או Python:
nohup python main.py &

# או PM2:
pm2 start ecosystem.config.js
```

**4️⃣ בדוק שהחשבון הנכון מחובר:**
```bash
curl -s "https://8080-ibu98pd4j6524ljwfdvht.e2b.dev/v1/api/iserver/accounts"
```

**📍 התוצאה הצפויה:**
```json
{
  "accounts": ["DU7428350"],
  "selectedAccount": "DU7428350"
}
```

### 📋 אופציה 3: עדכון קבצי תצורה

**אם יש קובץ `.env`:**
```bash
cat > .env << EOF
TWS_USERNAME=ilyuwc476
TWS_PASSWORD=trump123!
IBKR_ACCOUNT_ID=DU7428350
TRADING_MODE=paper
EOF
```

**אם יש `docker-compose.yml`:**
הוסף/עדכן:
```yaml
environment:
  - TWS_USERNAME=ilyuwc476
  - TWS_PASSWORD=trump123!
  - IBKR_ACCOUNT_ID=DU7428350
  - TRADING_MODE=paper
```

## ✅ אימות הצלחה

לאחר ההגדרה, בדוק בבוט Telegram:
- `/balance` - צריך להציג את היתרה האמיתית שלך (לא $50,000)
- `/portfolio` - צריך להציג את הפוזיציות האמיתיות (ללא TSLA)
- `/account` - צריך להציג DU7428350

## 🔧 אם זה לא עובד

**נסה פקודות נוספות:**
```bash
# הפעלה מחדש של מערכת
sudo systemctl restart your-service-name

# או בדיקת logs
docker logs container-name
pm2 logs
tail -f *.log

# או התקנה מחדש מלאה
git pull
npm install
docker-compose build --no-cache
docker-compose up -d
```

## 📞 תמיכה

אם אחרי כל זה עדיין לא עובד, שלח לי:
1. פלט של: `curl -s "https://8080-ibu98pd4j6524ljwfdvht.e2b.dev/v1/api/iserver/accounts"`
2. פלט של: `docker ps` או `pm2 status`
3. פלט של: `env | grep TWS`